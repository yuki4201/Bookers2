<%= render 'users/user',user:@book.user ,book:Book.new %>

<div class="col-md-8 offset-md-1">
<h2>Book detail</h2>
  <table class="table">
      <% if @user.id == current_user.id %>
      <% end %>
    <tbody>
        <td>
          <%= link_to user_path(@book.user) do %>
            <%= attachment_image_tag @user, :profile_image, format: 'jpeg', size:"40x40", fallback: "no_image.jpg" %>
            <br>
          　<%= @book.user.name %>
          <% end %>
        </td>
        <td><%= link_to @book.title %></td>
        <td><%= @book.body %></td>
        <% if @book.user == current_user %>
        <td><%= link_to "Edit", edit_book_path(@book.id), class: "btn btn-sm btn-success" %></td>
        <td><%= link_to "Destroy", book_path(@book.id), class: "btn btn-sm btn-danger", method: :delete, "data-confirm" => "Are you sure?" %></td>
        <% end %>
      </tr>
    </tbody>
  </table>